name: Prueba Automatizacion

on:
  workflow_dispatch:  # 2 espacios antes

jobs:
  prueba:             # 2 espacios antes
    runs-on: ubuntu-latest  # 4 espacios antes

    steps:            # 4 espacios antes
      - name: Hola Mundo  # 6 espacios antes
        run: echo "GitHub Actions funciona correctamente"
